struct Poly<const N: usize> {
    coef: [f64; N]
}

fn main() {
    print!("Hello, world!");
    //println!("Is it zero? {}", 1. / f64::INFINITY);
    //std::f64::consts::PI
    //std::f32::consts::PI
    //let mut v = vec![0; 10];
    //v.push(2);
    //let mut f = "first".to_string();
    //f = "ddd".to_string();
    //let mut v = vec![0; 10];
    //std::mem::rep

    //let ply = Poly<{5+1}>()
}